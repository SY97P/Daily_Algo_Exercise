def solution(w, h) : 
	answer = 0

	if w*

	# 대각선 기울기를 구해서 해당하는 좌표를 넣었을 때 어디로 오는지를 확인하자. 
	diag = h / w
	


def main() :
	w, h = 8,12		# 80
	# w, h = 2,2		# 2
	# w, h = 1, 1		# 0
	w, h = 1, 2		# 0
	print("solution : ", solution(w, h))

main()
